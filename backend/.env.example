# Default: local SQLite file, zero setup required.
DATABASE_URL="file:./dev.db"
PORT=3001

# To use Postgres instead (see docker-compose.yml at repo root):
# DATABASE_URL="postgresql://isleoflove:isleoflove@localhost:5432/isleoflove"
# and change the `provider` in prisma/schema.prisma to "postgresql"
